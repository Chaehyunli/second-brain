# SKALA Notion → Obsidian daily sync policy

- Source root: Notion `SKALA` page `39d1d84b-f68e-80f3-89b7-e70a6c911bf9`.
- Scope: recursively discover and sync all descendant pages. Do not follow pages outside this ancestor tree.
- Destination: `notion/SKALA/` in this Vault. Sync **only leaf pages containing learning material**. Determine hierarchy from an actual child-page relationship, not from arbitrary `<page>` references inside a page body; a linked reference alone does not make a page non-leaf. Do not create a SKALA index or any parent/hub note. User-confirmed learning leaf `[7/14] Git & AI코딩 & 환경구성 — 강의 정리` (`39d1d84b-f68e-8134-9337-f9f71471b3e3`) is always an eligible learning note. One eligible page maps to one Markdown note with `notion_page_id`, source URL, title, sync timestamp, and `content_sha256` in frontmatter. The filename uses the Notion title after replacing Windows-unsafe characters (`\\ / : * ? " < > |`) with `-`; use `notion_page_id` rather than filename as the stable identity.
- Skip non-learning administrative/orientation material, including the `[7/14] OT` page and attendance, mobile attendance, security pledge, schedule, campus, meal, operation, allowance, certificate, contact, and attendance-recognition information. SKALA notes contain learning material only.
- Preserve the eligible page's meaningful Markdown content and original Notion URL. Do not archive temporary signed image URLs as durable evidence; retain stable text and page references.
- Extract only the Notion `<content>` region, remove temporary signed-image URLs, normalize whitespace, then calculate `content_sha256` from that source text for a new note's provenance. **An uploaded eligible leaf is frozen after its first GitHub commit:** during the daily hook, add only an eligible learning leaf whose `notion_page_id` is not already present in `notion/SKALA/`. Do not update its body, title, filename, hash, timestamp, links, or Git history when the corresponding Notion page changes; do not delete it merely because its current Notion position or content changes. Reflect an existing page only after an explicit user request.
- Safety: stop before writing or committing if `git status --porcelain` reports unrelated local changes. Do not use reset or force push.
- Commit each newly added learning leaf separately and use its exact Notion title: `노션: SKALA - <페이지 제목> 업데이트`. Never create an automatic update or deletion commit for an already uploaded page.
- Push each successful commit to `origin/main`. If no page changed, make no commit and return no user-facing report.
